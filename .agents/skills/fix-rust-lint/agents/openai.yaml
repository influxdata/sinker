interface:
  display_name: "Fix Rust Lint"
  short_description: "Fix Sinker Rust lint and formatting issues"
  default_prompt: "Use $fix-rust-lint to fix Sinker Clippy and rustfmt findings while preserving behavior and running the required locked Cargo checks."
