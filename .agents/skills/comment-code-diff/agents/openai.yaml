interface:
  display_name: "Comment Code Diff"
  short_description: "Document scoped Rust code and YAML manifests"
  default_prompt: "Use $comment-code-diff to add concise, useful Rust documentation and YAML comments to this commit, branch, module, file, or manifest scope while preserving behavior."
